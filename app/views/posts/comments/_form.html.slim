= simple_form_for(comment, url: url) do |f|
  = f.input :content, label: 'Сomment'
  = f.input :parent_id, as: :hidden, input_html: { value: comment.id }
  = f.button :submit, class: 'btn btn-outline-primary me-2'